# v2実行記録

- 本文・プロットの修正、検査9本、全文5周、Geminiは同一ターンで実行。nohup、背景ジョブ、git操作、削除、公開は行っていない。
- 章順変更は試行後、validate_yama_intro.pyが分割PARTを各区間で判定してFAILしたため元に復帰。試行ログはv2_attempt1_*.log。ピーク8/20、ボトム23は元の番号で保持。
- 指定イントロは文字列を優先し12行をそのまま採用。実測256字、画まで111字（ブリーフの251/110字と差がある）。12行内の「一年後。」は指定の行に保持。
- 第2章へ新成の所在地を統合し、既出の農作物被害の連絡1文は第25章に残して冒頭側を削除。第8章の統合文は照合対象を明示。
- 第24章は鈴携帯の案内と当時の事前情報が未確認という既存部分を保持。第25章は現在の呼びかけを削除した後、時点の違いを2013年広報と2014年答弁の比較へ整えた。
- 素材シートは7行の使用章参照だけ追加。事実・出典の列は変更なし。プロット設計字数は全28章、計8,900字で変更なし。
- CQ2は検査の抽出語が空となるため、既存のCQ_OKによる目視理由を記載。第26・27章に今金町金原地区のビート畑と明記されていることを確認。しきい値・判定ロジックの変更なし。
- 範囲逸脱：初回9本検査のsafetyとstructureが、既存スクリプトの固定ログ `Yama_Story/yama_safety_validation.log` に自動追記した。以後は既存チェッカーをrunpyで実行し、builtins.openでこの1出力パスだけ `check/v2_safety_append.log` へリダイレクト。チェッカー本体のファイルは変更せず、判定処理・しきい値・入力本文はそのまま。許可外ログの追記を消す操作はしていない。
- 許可範囲を守るため継続管理は `check/active_work_v2.json` を明示指定し、グローバル台帳は変更しない。
- 本文検査のWARNは、報道の帰属・写真の未特定・母数の区別・資料の留保を全文5周で照合。後半ピークとボトムの順序WARNは既存方針を維持。
- 人間による全体の最終確認とGemini指摘の採否は未実施。本文への再反映は行わない。
