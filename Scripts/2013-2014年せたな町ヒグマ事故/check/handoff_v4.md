# v4 実行引継ぎ（未完了）

指定①〜⑤を反映。第24章は362字→240字。第27章の指定置換に伴う「でした」連続FAILは直前文を同じ事実の叙述へ変更して解消。
反映しない4件は不実装。第11・12・28章は作業前と完全一致し、第24章の指定された限定解釈3文を保持。
新しい事実の追加なし。素材シートは無変更。検査基準は無変更。

最終字数8,147字（253字不足）。使用77/79素材。未使用#65は削除指定、#46は外部素材で不使用指定。現行素材シートに未使用の採用可能な本件素材がない。
設計変更を明記し、第23章210字・第24章240字へ調整。実測欄も更新し、Check 11の乖離は0章。設計変更でも実測フロアのFAILは解消できない。
検査結果はv4_check_results.json、生出力はv4_*.log。Exit 0が7本、intro・plot・calibrationがExit 1。calibrationの新規失敗は本件introのみ。

次工程: Claude側で字数条件と削除・加筆制限の両立方法を設計する必要がある。採用可能な既存素材の具体箇所が指定されれば、その範囲で改稿して全検査を再実行する。
全文5周の文章チェックは未完了。最終本文の字数問題を解消後に新周期で実施し、旧版の5周記録を今回の完了証拠には使わない。
Gemini v4は全検査Exit 0の前提未達で未実行、費用0円。指摘_v4.mdは生成していない。旧v1〜v3は無変更。指摘件数・v3比較は未確認。
共通Geminiスクリプトの実体はSystem_Tools/review_script_gemini.py（前回v3の実行記録と一致）。再開時は同スクリプトを無改変で使用し、-oでv4用出力先を指定する。
継続管理・検査ログは対象内checkに保存。Safetyの固定ログ出力先のみ対象内へ迂回し、calibrationの子Safetyにも同じ処理を適用。検査内容は変えていない。
バックグラウンド処理・git・削除・外部送信なし。検査プロセスはすべて終了。
