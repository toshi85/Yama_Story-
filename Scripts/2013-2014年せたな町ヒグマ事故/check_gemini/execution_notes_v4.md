# Gemini v4 実行記録

- 10本全検査Exit 0と全文5周の完了後、同じ最終版で実行。
- 指定のYama_Story/System_Tools/review_script_gemini.pyは存在しないため、v3で使用したSystem_Tools/review_script_gemini.pyを無改変で実行した。出力先のみcheck_gemini/v4_rawへ指定し、完成後に指摘_v4.md・指摘_v4.jsonへ内容をそのまま保存。
- 起動から終了まで同じターンのフォアグラウンド実行。再試行なし。終了コード0。
- 指摘は内部第1周5件・第2周4件・第3周5件、計14件。内部第4周の未使用素材レビューは本件候補0件のためAPIを呼ばずスキップ。素材シート自体は存在し、認識されている。「素材シートが無い」は既存スクリプトの共通メッセージ。
- API使用量：入力40,972トークン、出力・思考9,667トークン。
- 費用：10.0470375円、表示10.05円。既存スクリプトの単価（入力$0.75/100万、出力$3.75/100万）・150円/米ドルで実測トークンから算出。請求明細との照合ではない。
- v3の14件から合計件数は変わらない。個別比較は差分_v4.md。
- 本文・素材シート・プロット・レビュー用スクリプト、v1〜v3を含む既存出力26ファイルのハッシュは実行前後で一致。今回のGeminiの指摘による本文変更なし。合否・採否は未判定。
